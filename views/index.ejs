<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Search By Name</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="firstName" type="text" class="ghost-input" placeholder="Enter your First Name" required>
          <input name="lastName" type="text" class="ghost-input" placeholder="Enter your Last Name" required>
          <input type="submit" class="ghost-button" value="Get Data">
        </form>
        <% if(data !== null){ %>
          <p><%= data %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>